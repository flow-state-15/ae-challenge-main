import { Account } from "../../globalTypes";

export function validateWithdrawRequest(amount: number, account: Account) {
    // check is positive non-zero
    // check less than 201
    // check less than 401 in single day
    // check is divisible by 5
    // check account has enough funds
    // 
}